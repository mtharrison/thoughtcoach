{
  "name": "serverless-user-feedback",
  "version": "0.0.0",
  "private": true,
  "main": "functions.js",
  "scripts": {
    "dev": "npx sls offline",
    "dev-deploy": "sls deploy --org=mtharrison --stage dev",
    "prd-deploy": "sls deploy --org=mtharrison --stage prd",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "None",
  "dependencies": {
    "aws-sdk": "^2.1357.0"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  }
}
