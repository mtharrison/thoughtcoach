{
  "name": "thought-coach-openai-api",
  "version": "0.0.0",
  "private": true,
  "main": "functions.js",
  "scripts": {
    "dev": "npx sls offline",
    "dev-deploy": "sls deploy --org=mtharrison --stage dev",
    "prd-deploy": "sls deploy --org=mtharrison --stage prd",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "None",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1359.0",
    "openai": "^3.2.1",
    "sst": "^2.5.2"
  },
  "devDependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.310.0"
  }
}
